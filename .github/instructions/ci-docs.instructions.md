---
applyTo: ".github/**,README.md,*.md,.cursor/**"
---

# CI and Docs Instructions

- 根 `README.md` 只写游戏玩法、CI 发布和世界观简述，详细世界观链接到 `世界观设定.md`。
- 根 AI 指令只写架构；模块细节放到对应目录或 `.github/instructions/`。
- PR preview 工作流发布到 `gh-pages` 的 `pr-preview/pr-<number>/`。
- 不把旧平级目录路径写入新文档，除非明确说明历史迁移。
