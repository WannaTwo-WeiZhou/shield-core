---
description: ShieldCore 项目规范 - 分层 AI 指令入口
globs: *
alwaysApply: true
---

# ShieldCore 项目规范

始终使用简体中文回复用户；代码、命令、日志、API 名称和路径保留原文。

本仓库的 AI 指令采用分层维护：

- Codex / 通用代理：根路径和子目录的 `AGENTS.md`
- Claude Code：根路径和子目录的 `CLAUDE.md`
- GitHub Copilot：`.github/copilot-instructions.md` 与 `.github/instructions/*.instructions.md`

根路径指令只描述项目架构；模块细节以对应目录的指令文件为准。

## 项目架构

- `core/`：Autoload 和全局基础设施。
- `gameplay/`：能力、玩家、战斗、成长等玩法实现。
- `ui/`：开始、暂停、游戏结束等流程界面。
- `assets/`：跨模块共享资源。
- `main.tscn`：主场景入口。

## Godot LSP（MCP）

涉及 `.gd` 代码时优先使用可用的 Godot / GDScript LSP 能力进行查定义、查引用、诊断和重命名；纯文本搜索再使用 `rg`。

修改 `.gd` 后，在继续语义查询前同步或重新打开相关文件，避免诊断和引用结果过期。

完整模块规则请读取当前目录最近的 `AGENTS.md` / `CLAUDE.md`，Copilot 场景请读取 `.github/instructions/` 中匹配路径的文件。
